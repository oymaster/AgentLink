# AgentLink local demo configuration.
#
# Copy this file to .env locally if you use a shell that loads dotenv files,
# or export the variables before running demo binaries.

# Redis settings used by remote-agent demos and integration tests.
AGENTLINK_REDIS_HOST=127.0.0.1
AGENTLINK_REDIS_PORT=6379
